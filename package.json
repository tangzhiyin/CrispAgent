{
  "name": "crisp-iphone-agent",
  "version": "0.1.0",
  "private": true,
  "description": "An installable iPhone agent that runs the local crisp-voice skill through GitHub Copilot SDK.",
  "type": "module",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "test": "node --test",
    "check": "node --check src/server.js && node --check src/agent-runtime.js && node --check src/app-server.js && node --check public/app.js && node --check public/sw.js",
    "smoke": "node scripts/smoke-agent.js"
  },
  "dependencies": {
    "@github/copilot-sdk": "1.0.9-preview.3"
  }
}
