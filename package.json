{
  "name": "crisp-iphone-agent",
  "version": "0.1.0",
  "private": true,
  "description": "A native offline iPhone agent for Gemma 4 and importable prompt skills, with an optional Copilot gateway.",
  "type": "module",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "test": "node --test",
    "check": "node --check src/server.js && node --check src/agent-runtime.js && node --check src/app-server.js && node --check public/app.js && node --check public/sw.js && node scripts/check-ios-source.js",
    "smoke": "node scripts/smoke-agent.js",
    "ios:icon": "node scripts/generate-ios-icon.js"
  },
  "dependencies": {
    "@github/copilot-sdk": "1.0.9-preview.3"
  }
}
